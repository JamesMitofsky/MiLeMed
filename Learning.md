# User management
`useUser.ts`


# Change log
* Add column manually in Supabase for the role type. Define this test account as admin. Types are defined in `types.ts`
* Do complete replace of "category_id" with "chapter_id" and add this as a FK connection in supabase portal.